clear all; clc;

number = 0;
record = 0;
for i = 1: 1000000
    if mod(i, 100)
        disp(i)
    end
    score = e013_counter_function(i);
    if score > record
        record = score;
    end
end

disp(record)
